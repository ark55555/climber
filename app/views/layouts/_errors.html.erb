<% if resource.errors.any? %>
	<div class="alert-danger">
		<h6>＜エラー＞下記項目をご確認ください</h6>
		<ul>
			<% resource.errors.full_messages.each do |message| %>
			<li><%= message %></li>
			<% end %>
		</ul>
	</div>
<% end %>