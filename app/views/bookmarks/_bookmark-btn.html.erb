<% if post.bookmarked_by?(current_user) %>
  <%= link_to post.bookmarks.count, post_bookmarks_path(post), method: :delete, class: "fas fa-bookmark" %>
<% else %>
  <%= link_to post.bookmarks.count, post_bookmarks_path(post), method: :post, class: "far fa-bookmark" %>
<% end %>